<view class="container">
  <button type="default" bindtap="getLocation">获取定位</button>
  <view>经度：{{locationInfo.longitude}}</view>
  <view>纬度：{{locationInfo.latitude}}</view>
  <view>速度：{{locationInfo.speed}}</view>
  <view>高度：{{locationInfo.altitude}}</view>
</view> 